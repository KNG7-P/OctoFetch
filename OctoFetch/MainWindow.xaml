<Window x:Class="OctoFetch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OctoFetch CLOUD ENGINE" Height="820" Width="1200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernSmallButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#2D2D40" CornerRadius="4" Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3D3D55"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#606070"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Width="30" Height="30">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E53935"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonToggle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Cursor="Hand" Margin="0,5">
                            <Border x:Name="BgBorder" Width="40" Height="22" CornerRadius="11" Background="#1E1E2C" BorderBrush="#2D2D40" BorderThickness="1">
                                <Ellipse x:Name="Dot" Width="14" Height="14" Fill="#606070" HorizontalAlignment="Left" Margin="3,0,0,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="DotTrans"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" TextElement.Foreground="#A0A0B0" TextElement.FontSize="13"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="#1A3A3A"/>
                                <Setter TargetName="BgBorder" Property="BorderBrush" Value="#00E5FF"/>
                                <Setter TargetName="Dot" Property="Fill" Value="#00E5FF"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="DotTrans" Storyboard.TargetProperty="X" To="18" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="DotTrans" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavMenuButton" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="NavBorder" Background="Transparent" CornerRadius="8" Padding="15,12" Margin="0,5" Cursor="Hand" BorderThickness="4,0,0,0" BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" TextElement.Foreground="#808090" TextElement.FontSize="14" TextElement.FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="#1A1A24"/>
                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="#1E1E2C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#0A0A10" BorderBrush="#1C1C28" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#12121A" CornerRadius="15,0,0,15" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="OCTO FETCH" Foreground="#FFFFFF" FontSize="26" FontWeight="Black" Margin="0,0,0,2"/>
                        <TextBlock Text="V 1.0" Foreground="#00E5FF" FontSize="11" FontWeight="Bold" Margin="0,0,0,30"/>

                        <RadioButton x:Name="NavDownloader" Content="🚀  Dashboard" Style="{StaticResource NavMenuButton}" IsChecked="True" Checked="Nav_Checked"/>
                        <RadioButton x:Name="NavFileManager" Content="📁  Cloud Storage" Style="{StaticResource NavMenuButton}" Checked="Nav_Checked"/>
                        <RadioButton x:Name="NavExtractor" Content="🗜️  Zip Extractor" Style="{StaticResource NavMenuButton}" Checked="Nav_Checked"/>
                        <Border BorderBrush="#2D2D40" BorderThickness="0,1,0,0" Margin="10,15,10,15"/>
                        <RadioButton x:Name="NavSettings" Content="⚙️  Node Management" Style="{StaticResource NavMenuButton}" Checked="Nav_Checked"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <Border Background="#0A0A10" CornerRadius="10" Padding="15" BorderBrush="#2D2D40" BorderThickness="1" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="GITHUB STATUS" Foreground="#808090" FontSize="10" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock x:Name="TxtSidebarStatus" Text="Offline" Foreground="#E53935" FontSize="13" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtActiveNodes" Text="0 Nodes Connected" Foreground="#808090" FontSize="11" Margin="0,0,0,10"/>
                                <TextBlock Text="Load Balancing: Active" Foreground="#808090" FontSize="10" Margin="0,0,0,5"/>
                            </StackPanel>
                        </Border>

                        <Button x:Name="BtnTelegram" Style="{StaticResource ModernButton}" Background="#2AABEE" Padding="0,12" Click="BtnTelegram_Click">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="✈️" Foreground="#FFFFFF" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Join @King_network7" Foreground="#FFFFFF" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="25,20,25,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="✕" HorizontalAlignment="Right" Style="{StaticResource CloseButton}" FontSize="15" FontWeight="Bold" Margin="0,-5,-5,0" Click="BtnClose_Click"/>

                <Grid x:Name="GridDownloader" Grid.Row="1" Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="SOURCE FILE URL" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                        <Border CornerRadius="10" Background="#12121A" BorderBrush="#2D2D40" BorderThickness="1" Margin="0,0,0,15">
                            <TextBox x:Name="TxtTargetUrl" Background="Transparent" Foreground="#00E5FF" BorderThickness="0" Padding="15" FontSize="13"/>
                        </Border>
                        <Grid>
                            <StackPanel HorizontalAlignment="Left">
                                <CheckBox x:Name="ChkSafeName" Content="Clean Filename (Remove spaces &amp; special chars)" Style="{StaticResource NeonToggle}" IsChecked="True"/>
                                <CheckBox x:Name="ChkEncryptName" Content="Hash Filename (Hide original name)" Style="{StaticResource NeonToggle}"/>
                            </StackPanel>
                            <Button x:Name="BtnStartLeech" Content="⚡ START TRANSFER" Style="{StaticResource ModernButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#00E5FF" Foreground="#0A0A10" FontWeight="Black" FontSize="14" Padding="35,16" Click="BtnStartLeech_Click"/>
                        </Grid>
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,25,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="5*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#12121A" CornerRadius="12" Padding="15" BorderBrush="#2D2D40" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="PROCESS LOGS" Foreground="#808090" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ListBox x:Name="ListLogs" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Message}" Foreground="{Binding Color}" TextWrapping="Wrap" FontSize="12" FontFamily="Consolas" Margin="0,4"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Button x:Name="BtnClearLogs" Grid.Row="2" Content="🗑️ Clear Logs" Style="{StaticResource ModernButton}" Background="#2D2D40" Foreground="#A0A0B0" FontWeight="Bold" Padding="0,10" Margin="0,10,0,0" Click="BtnClearLogs_Click"/>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="#0F1A24" CornerRadius="12" Padding="15" BorderBrush="#00E5FF" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <TextBlock Text="READY TO DOWNLOAD" Foreground="#00E5FF" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnFetchMissing" Content="🔄 Force Check" HorizontalAlignment="Right" Style="{StaticResource ModernSmallButton}" Foreground="#00E5FF" Click="BtnFetchMissing_Click"/>
                                </Grid>

                                <ListBox x:Name="ListLinks" Grid.Row="1" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#1A2A3A" CornerRadius="8" Padding="12" Margin="0,4" BorderBrush="#2D4050" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding FileName}" Foreground="#FFFFFF" TextTrimming="CharacterEllipsis" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                                    <Button Grid.Column="1" Content="📋 Copy URL" Tag="{Binding Url}" Click="BtnCopySingleLink_Click" Style="{StaticResource ModernSmallButton}" Foreground="#00E5FF" FontSize="11" FontWeight="Bold"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Button x:Name="BtnCopyAll" Grid.Row="2" Content="📋 COPY ALL LINKS TO CLIPBOARD" Style="{StaticResource ModernButton}" Background="#00E5FF" Foreground="#0A0A10" FontWeight="Bold" Padding="0,14" Margin="0,15,0,0" Click="BtnCopyAll_Click"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>

                <Grid x:Name="GridFileManager" Grid.Row="1" Margin="0,15,0,0" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="CLOUD STORAGE" Foreground="#FFFFFF" FontSize="22" FontWeight="Black" VerticalAlignment="Center"/>
                        <Button x:Name="BtnRefreshFiles" Content="🔄 SYNC CLOUD" Style="{StaticResource ModernButton}" HorizontalAlignment="Right" Background="#2D2D40" Foreground="#FFFFFF" FontWeight="Bold" Padding="20,10" Click="BtnRefreshFiles_Click"/>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="BtnBackFolder" Grid.Column="0" Content="🔙 Directory Up" Style="{StaticResource ModernButton}" Background="#2D2D40" Foreground="#FFFFFF" FontWeight="Bold" Padding="15,6" Margin="0,0,15,0" Visibility="Collapsed" Click="BtnBackFolder_Click"/>
                        <TextBlock x:Name="TxtCurrentPath" Grid.Column="1" Text="📁 Global Root" Foreground="#00E5FF" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>

                    <Border Grid.Row="2" Background="#12121A" CornerRadius="12" Padding="10" BorderBrush="#2D2D40" BorderThickness="1">
                        <ListView x:Name="ListViewFilesGithub" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" SelectionMode="Extended" MouseDoubleClick="ListViewFiles_MouseDoubleClick">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1A1A24" CornerRadius="8" Padding="15,12" Margin="0,3" BorderBrush="#2D2D40" BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" Foreground="#FFFFFF" FontWeight="Bold" FontSize="14"/>
                                                <TextBlock Text="{Binding SubText}" Foreground="#808090" FontSize="11" Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button x:Name="BtnOpenFolder" Content="📂 Open" Style="{StaticResource ModernButton}" Background="#8A2BE2" Foreground="#FFFFFF" FontWeight="Bold" FontSize="13" Padding="30,12" Margin="0,0,10,0" Click="BtnOpenFolder_Click"/>
                        <Button x:Name="BtnCopySelected" Content="📋 Copy Links" Style="{StaticResource ModernButton}" Background="#2D2D40" Foreground="#00E5FF" FontWeight="Bold" FontSize="13" Padding="30,12" Margin="0,0,10,0" Click="BtnCopySelected_Click"/>
                        <Button x:Name="BtnDeleteSelected" Content="🗑️ Delete" Style="{StaticResource ModernButton}" Background="#E53935" Foreground="#FFFFFF" FontWeight="Bold" FontSize="13" Padding="30,12" Click="BtnDeleteSelected_Click"/>
                    </StackPanel>
                </Grid>

                <Grid x:Name="GridExtractor" Grid.Row="1" Margin="0,15,0,0" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="ZIP PART EXTRACTOR" Foreground="#FFFFFF" FontSize="22" FontWeight="Black" Margin="0,0,0,25"/>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Select the primary split file (.zip.001):" Foreground="#808090" FontSize="13" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" CornerRadius="10" Background="#12121A" BorderBrush="#2D2D40" BorderThickness="1" Margin="0,0,15,0">
                                <TextBox x:Name="TxtExtractPath" Background="Transparent" Foreground="#FFFFFF" BorderThickness="0" Padding="15" FontSize="13" IsReadOnly="True"/>
                            </Border>
                            <Button x:Name="BtnBrowse" Grid.Column="1" Content="📂 BROWSE FILES" Style="{StaticResource ModernButton}" Background="#2D2D40" Foreground="#FFFFFF" FontWeight="Bold" Padding="30,0" Click="BtnBrowse_Click"/>
                        </Grid>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#12121A" CornerRadius="12" Padding="15" BorderBrush="#2D2D40" BorderThickness="1" Margin="0,25,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="SYSTEM LOGS" Foreground="#808090" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                            <ListBox x:Name="ListExtractorLogs" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="#A0A0B0" FontSize="13" FontFamily="Consolas"/>
                        </Grid>
                    </Border>

                    <Button x:Name="BtnExtract" Grid.Row="3" Content="🗜️ START EXTRACTION" Style="{StaticResource ModernButton}" Background="#00E676" Foreground="#0A0A10" FontWeight="Black" Padding="0,18" Margin="0,25,0,0" FontSize="15" IsEnabled="False" Click="BtnExtract_Click"/>
                </Grid>

                <Grid x:Name="GridSettings" Grid.Row="1" Margin="0,15,0,0" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="NODE MANAGEMENT" Foreground="#FFFFFF" FontSize="22" FontWeight="Black" Margin="0,0,0,20"/>

                    <Border Grid.Row="1" Background="#12121A" CornerRadius="12" Padding="25" BorderBrush="#2D2D40" BorderThickness="1" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Personal Access Token (PAT):" Foreground="#808090" FontSize="12" Margin="0,0,0,8"/>
                                <Border CornerRadius="8" Background="#0A0A10" BorderBrush="#2D2D40" BorderThickness="1">
                                    <TextBox x:Name="TxtNewToken" Background="Transparent" Foreground="#FFFFFF" BorderThickness="0" Padding="12" FontFamily="Consolas"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Repository Name:" Foreground="#808090" FontSize="12" Margin="0,0,0,8"/>
                                <Border CornerRadius="8" Background="#0A0A10" BorderBrush="#2D2D40" BorderThickness="1">
                                    <TextBox x:Name="TxtNewRepo" Text="Cloud-Downloads" Background="Transparent" Foreground="#FFFFFF" BorderThickness="0" Padding="12" FontWeight="Bold"/>
                                </Border>
                            </StackPanel>
                            <Button x:Name="BtnAddNode" Grid.Column="4" Content="➕ ADD NODE" Style="{StaticResource ModernButton}" Background="#8A2BE2" Foreground="#FFFFFF" FontWeight="Bold" Padding="30,0" Margin="0,24,0,0" Click="BtnAddNode_Click"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="350"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#12121A" CornerRadius="12" Padding="15" BorderBrush="#2D2D40" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="ACTIVE NODES" Foreground="#00E5FF" FontSize="12" FontWeight="Bold" Margin="5,0,0,15"/>

                                <ListView x:Name="ListViewNodes" Grid.Row="1" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#1A1A24" CornerRadius="8" Padding="10" Margin="0,4" BorderBrush="#2D2D40" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Ellipse Grid.Column="0" Fill="{Binding BadgeColor}" Width="12" Height="12" Margin="5,0,15,0" VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding DisplayTitle}" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" Text="{Binding VisibilityText}" Foreground="#808090" FontSize="11" Margin="15,0" VerticalAlignment="Center"/>

                                                    <Button Grid.Column="3" Content="👁️" Tag="{Binding}" Click="BtnToggleNodeVisibility_Click" Style="{StaticResource ModernSmallButton}" Foreground="#FFFFFF" Margin="0,0,10,0"/>
                                                    <Button Grid.Column="4" Content="🗑️" Tag="{Binding}" Click="BtnRemoveNode_Click" Style="{StaticResource ModernSmallButton}" Foreground="#E53935"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                                    <Button x:Name="BtnSaveSettings" Content="💾 SAVE" Style="{StaticResource ModernButton}" Background="#2D2D40" Foreground="#FFFFFF" FontWeight="Bold" Padding="25,12" Margin="0,0,10,0" Click="BtnSaveSettings_Click"/>
                                    <Button x:Name="BtnTestNodes" Content="🔄 TEST ALL" Style="{StaticResource ModernButton}" Background="#00E676" Foreground="#0A0A10" FontWeight="Bold" Padding="25,12" Click="BtnTestNodes_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="#0A0A10" CornerRadius="12" Padding="20" BorderBrush="#2D2D40" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="CONNECTION DIAGNOSTICS" Foreground="#808090" FontSize="11" FontWeight="Bold" Margin="0,0,0,15"/>
                                <ListBox x:Name="ListSettingsLogs" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="#A0A0B0" FontSize="12" FontFamily="Consolas" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,4"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>